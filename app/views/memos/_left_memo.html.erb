<% if user_signed_in? %>
    <div class="left-side">
        <% @memos.each do |memo|%>
            <% if current_user.id == memo.user_id  %>
                <%= link_to memo.title, memo_path(memo) %>
            <% end %> 
        <% end %>
    </div>
<% end %>